config:
  target: http://localhost:8080
  phases:
    - duration: 30
      arrivalRate: 10
      name: benchmark
scenarios:
  - flow:
    - get:
        url: "/benchmark?n=1"
        expect:
          - equals:
            - "OK"